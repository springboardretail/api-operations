// just an exporter for the modules
// here we can control the default external API of the package
import {
  get,
  getQuery,
  postJson,
  putJson,
  patchJson,
  sendJson,
  delete_,
  createApiSource,
} from './apiOperations'

export {
  get,
  getQuery,
  postJson,
  putJson,
  patchJson,
  sendJson,
  delete_,
  createApiSource,
}
